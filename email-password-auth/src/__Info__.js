/**
 * 1. First time visit  No Account before
 * SignUp == create account == register
 *
 *
 *
 * 2.ALready Have an account
 * SignIn == Login
 */

// firebase setup basic steps needed to follow must
/**
 * 1.firebase project start
 * 2.create web app
 * 3.Install firebase in the project
 * 4.get config
 * 5.Authentication method  enable ; Email / Password
 * 6.go to doc and intilize auth : Firebase  Docs > Build > Authentication > Web > Get Started
 * 7.import { getAuth } from "firebase/auth";
 * 8.export const auth = getAuth(app);
 * 9.Specific authentication method : Email/password 


*/

/**
 * 1.create a FireBase project
 * 2.Build > authentication > google sign in -- enable
 * 3.Project settings : web application
 * 4.FireBase install
 * 5.Config copy
 * 6.int the config : init the auth of firebase application
 */
